#as used to give a module or function as nickname for easier usage
import numpy as np  # We can use any nickname but np is better then oers because this is in common use b/w devolopers  
arr=np.numpy([1,2,3])
print(np) #
# we creata nickname for easy understanding