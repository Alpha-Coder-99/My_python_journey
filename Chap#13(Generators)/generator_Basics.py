# A Generator is a special function that returns an iterator.
#  It allows you to declare a function that behaves like an iterator, meaning it can be used in a for loop.
#  It uses the yield keyword instead of return to produce data lazily, saving significant memory.
